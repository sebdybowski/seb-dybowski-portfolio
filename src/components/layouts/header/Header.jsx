import React from 'react';

import { Navbar } from '../../navigation';
import { SPACING, BRAND, TYPOGRAPHY } from '../../../styles';

export const Header = () => (
  <header>
    <Navbar />
  </header>
);
